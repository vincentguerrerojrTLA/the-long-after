# Sitemap groundwork

The TLA rebuild has stable public page paths:
- /index.html
- /game.html
- /world.html
- /survival-guide.html
- /media.html
- /development.html

A production sitemap.xml and canonical URL tags must be generated only after the canonical public domain is confirmed. Do not invent a domain in metadata.
